import React from 'react';
import Navigation from '../components/Navigation';

const About = (props) => {
    return (
        <div>
            <Navigation />
            <h1>A propos</h1>
        </div>
    );
};

export default About;